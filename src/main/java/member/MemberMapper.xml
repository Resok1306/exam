<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member.MemberMapper">

<insert id="save" parameterType="member.Member">
insert into member(id, name, username,password,email) 
values(member_seq.nextval,#{name},#{username},#{password},#{email})
</insert>

<select id="findByUsername" resultType="member.Member">
select username, password from member where username=#{username}
</select>

<select id="countByUsername" resultType="int">
    SELECT COUNT(*) FROM member WHERE username = #{username}
</select>

<select id="countByEmail" resultType="int">
    SELECT COUNT(*) FROM member WHERE email = #{email}
</select>
</mapper>